APP_ENV=development
# Local Postgres (see docker-compose.yml). Plain libpq URL; the app switches it to asyncpg.
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/strs_training
CORS_ORIGINS=*
LOG_LEVEL=
